PROCEDURE alice
  n := nodes count
  m := message length

  basesMartix := bool[m][n] # a 2-dimension array to store the basis everyone use to measure qubits
  measurementResult := [] # container of measurement result of Alice
  
  # receive qubits from tp and measures them
  


END PROCEDURE
